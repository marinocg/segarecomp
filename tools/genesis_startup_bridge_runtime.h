#ifndef SEGARECOMP_GENESIS_STARTUP_BRIDGE_RUNTIME_FORWARD_H
#define SEGARECOMP_GENESIS_STARTUP_BRIDGE_RUNTIME_FORWARD_H

/* Compatibility include only. The strict-C11 runtime implementation is owned
 * by platforms/genesis/runtime/runtime.{h,c}; new consumers must include runtime.h. */
#include "../platforms/genesis/runtime/runtime.h"

#endif
